# Solo para desarrollo local — publica puertos en localhost.
# Uso: docker compose -f docker-compose.yml -f docker-compose.local.yml up --build

services:
  back:
    ports:
      - "4000:4000"
    env_file:
      - .env

  front:
    ports:
      - "3000:3000"
